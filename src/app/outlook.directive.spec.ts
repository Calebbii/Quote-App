import { OutlookDirective } from './outlook.directive';

describe('OutlookDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlookDirective();
    expect(directive).toBeTruthy();
  });
});
